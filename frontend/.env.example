# Variáveis VITE_ ficam públicas no bundle. Nunca coloque segredos aqui.
VITE_API_BASE_PATH=/api

# Somente desenvolvimento/preview local; não é exposta ao bundle da aplicação.
VITE_DEV_API_ORIGIN=http://127.0.0.1:3000
